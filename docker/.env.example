# Copy this file to docker/.env and fill in real values before running docker compose.
# Never commit docker/.env to version control.

# PostgreSQL password (required)
# Set a strong random password for local dev and a different one for production.
POSTGRES_PASSWORD=change_me_before_use
